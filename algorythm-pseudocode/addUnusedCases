addUnusedCases = function(useCases,functions){
	for(function in functions){
		if(useCases.includes(useCase =>(
			useCase.createdFromFlow.target == f		
		))){
			continue;
		}
		useCase = createUnusedUsecase(function);
		useCases.push(useCase);
	}
}


